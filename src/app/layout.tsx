import './globals.css';

export const metadata = {
  title: 'TransformArte',
  description: 'Art and mental health awareness initiative',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 